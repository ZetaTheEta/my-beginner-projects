from pickle import FALSE
import pickle
import time
import ipywidgets as widgets
from IPython.display import display


#Name of this script: Unfinished Password Script


# (question.lower() == "Michelle".lower()):


def check_password(input_password):
  correct_password = "123"
  if input_password == correct_password:
    return True
  else:
    return False

username = input("What is your name: ")





def correct_password(gender):
  while True:
    gender = input(f"What is your sex (Female, Male, Other): ")
    if (gender.lower() != f"Female".lower()) and (gender.lower() != f"Male".lower()) and (gender.lower() != f"Other".lower()):
      print(f"Invalid Option. Your options are: Female, Male, Other ")
      time.sleep(0.5)
    else:
      time.sleep(0.5)
      while True:
        try:
          age = int(input(f"What is your age: "))
          break
        except ValueError:
          print(f"Please enter a valid age (numbers only).")
      break
  time.sleep(0.5)
  while True:
    nationality = input(f"What is your nationality: ")
    if (nationality.lower() != f"Asian".lower ()) and (nationality.lower() != f"African".lower())  and (nationality.lower() != f"North American".lower())  and (nationality.lower() != "South American".lower()) and (nationality.lower() != "Antarctican".lower()) and (nationality.lower() != "European".lower())  and (nationality.lower() != "Australian".lower()):
      print(f"Invalid Option. Your options are: Asian, African, North American, South American, Antarctican, European, Australian")
      time.sleep(0.5)
    else:
      time.sleep(0.5)
      print(f"You have completed the fillout, please review your information before moving on. ")
      time.sleep(0.5)
      nation = [nationality]
      name = [username]
      age = [age]
      gender = [gender]
      print("Name: ", name)
      print("Nationality: ", nation)
      print("age: ", age)
      print("gender: ", gender)
      time.sleep(1)
      print(f"Is this correct? ")
      answer = input(f"")
      if age[0] < 17:
        time.sleep(1)
        print(f"Welcome, {username}, user is under 17")
        return True
      else:
        time.sleep(1)
        print(f"Welcome, {username}, user is 17+")
        return True







for attempt in range(3):
  user_input = input("Enter the password: ")
  if check_password(user_input):
    print(f"Correct password, welcome '{username}'")
    time.sleep(0.5)
    correct_password(gender = "")
    break
  else:
    print(f"Incorrect password, you have {2 - attempt}, chances remaining.")

else: print(f"You have been locked out. Please try again later.")
